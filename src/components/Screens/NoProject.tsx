import React from 'react';
import {} from '../Elements';
import { Screen } from './Screen';
import { InfoCard } from '../Elements';

export const NoProject = () => {
  return <Screen content={<InfoCard variant="no project" />} />;
};
